steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '--tag=$_REGION-docker.pkg.dev/$PROJECT_ID/demo/cbombard:$_IMG_TAG', '.']
substitutions:
  _IMG_TAG: v1 #default
images: ['$_REGION-docker.pkg.dev/$PROJECT_ID/demo/cbombard:$_IMG_TAG']

